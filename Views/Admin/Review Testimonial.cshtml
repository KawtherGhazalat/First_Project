@* @model IEnumerable<First_Project.Models.Testimonial>

@{
    ViewData["Title"] = "ReviewTestimonial";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1>Review Testimonial</h1>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.State)
            </th>
            <th>
                Feedback Message
            </th>
            <th>
                User Name
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.State)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Content)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Userid)
                </td>
                <td>
                    <a href="@Url.Action("AcceptTestimonial", "Admin", new { id = item.Userid })">Accept</a> |
                    <a href="@Url.Action("RejectTestimonial", "Admin", new { id = item.Userid })">Reject</a>
                </td>
                
            </tr>
        }
    </tbody>
</table> *@